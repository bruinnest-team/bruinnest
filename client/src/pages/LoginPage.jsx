function LoginPage() {
  return (
    <main className="page-shell">
      <section className="page-card">
        <p className="page-eyebrow">Auth</p>
        <h1>Login</h1>
        <p>Vite + React initialization is complete. Login form wiring comes next.</p>
      </section>
    </main>
  );
}

export default LoginPage;
